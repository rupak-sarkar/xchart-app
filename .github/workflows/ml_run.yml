name: ML Model Training

on:
  schedule:
    # Run every Saturday at 6 AM UTC (11:30 AM IST)
    - cron: '0 6 * * 6'
  workflow_dispatch:

jobs:
  ml-train:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install pandas numpy yfinance lightgbm

      - name: Run ML pipeline
        run: python ml_run.py

      - name: Commit ML results
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add ml_predictions.csv
          git diff --staged --quiet || git commit -m "🤖 ML predictions update $(date -u +%Y-%m-%d)"
          git push
