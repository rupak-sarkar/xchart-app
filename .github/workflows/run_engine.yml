name: Run Prediction Engine

on:
  schedule:
    - cron: '30 4 * * 1-5'
  workflow_dispatch:

jobs:
  run-engine:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install pandas numpy yfinance feedparser requests beautifulsoup4 transformers torch

      - name: Cache HuggingFace models
        uses: actions/cache@v4
        with:
          path: ~/.cache/huggingface
          key: hf-finbert-v1

      - name: Run engine
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: python app.py

      - name: Verify output
        run: |
          echo "=== Checking generated files ==="
          ls -la data.csv meta.json 2>/dev/null || echo "MISSING: data.csv or meta.json"
          ls charts/ | head -5 2>/dev/null || echo "MISSING: charts/ directory"
          echo "=== Chart files count ==="
          ls charts/*.json 2>/dev/null | wc -l || echo "0 chart files"

      - name: Commit and push
        run: |
          find . -type d -name __pycache__ -exec rm -rf {} + 2>/dev/null || true
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data.csv meta.json history.csv charts/
          git diff --staged --quiet || git commit -m "🔄 Engine update $(date -u +%Y-%m-%d_%H:%M)"
          git pull --rebase origin master || git pull origin master
          git push
